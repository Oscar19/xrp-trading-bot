

- name: Ejecutar backtest
  env:
    BITGET_SYMBOL: ${{ github.event.inputs.symbol || 'SOL/USDT' }}
    DIRECCION: ${{ github.event.inputs.direccion || 'both' }}
    LEVERAGE: '5'
    VOLUMEN_MIN_RATIO: '1.5'
  run: python bot_rsi_canales_v10_3.py
